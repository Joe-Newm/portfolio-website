---

---

<nav>
    <div>
        <!-- <p class="text-gray-600">j.newm500@gmail.com</p> -->
        <iframe src="/logo.svg" width="30" height="25"></iframe>
    </div>
    <!-- <div class="flex items-center space-x-4">
        <label for="dark-mode-toggle" class="flex items-center cursor-pointer">
          <div class="relative">
            <input id="dark-mode-toggle" type="checkbox" class="sr-only">
            <div class="block bg-gray-600 w-14 h-8 rounded-full"></div>
            <div class="dot absolute left-1 top-1 bg-white w-6 h-6 rounded-full transition transform"></div>
          </div>
        </label>
      </div> -->
    <div class="flex">
        <a
            class={`text-gray-600 transition hover:text-black ${Astro.url.pathname === "/" ? "active" : ""}`}
            href="/"
        >
            Home
        </a>
        <p class="text-gray-600 separator">/</p>
        <!-- <a class="text-gray-600" href="/about/">About / </a> -->
        <a
            class={`text-gray-600 transition hover:text-black ${Astro.url.pathname === "/blog/" ? "active" : ""}`}
            href="/blog/"
        >
            Blog
        </a>
        <p class="text-gray-600 separator">
            / <p>
                <a
                    class="text-gray-600 transition hover:text-black"
                    href="/documents/Joseph_Newman_Resume_v5.pdf"
                    target="_blank"
                >
                    Resume</a
                >
            </p>
        </p>

        <style>
            .active {
                color: #000;
            }
            .separator {
                padding: 0 5px; /* Adjust the padding to your preference */
            }
            nav {
                margin-top: 75px;
                margin-left: auto;
                margin-right: auto;
                margin-bottom: 50px;
                padding: 0px 40px;
                width: calc(100% - 640px);
                max-width: 900px;
                min-width: 800px;
                display: flex;
                justify-content: space-between;
            }

            /* For small screens, adjust the layout */
            @media (max-width: 700px) {
                nav {
                    min-width: 100%; /* Let it go full width on smaller screens */
                    padding: 0 20px; /* Adjust padding for smaller devices */
                }
            }
        </style>
    </div>
</nav>
